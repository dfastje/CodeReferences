#Audiobookshelf
# https://www.audiobookshelf.org/docs/#docker-compose-install
# https://hub.docker.com/r/advplyr/audiobookshelf


services:
  audiobookshelf:
    image: advplyr/audiobookshelf:latest
    ports:
      - 8011:80
    volumes:
      - /home/eyeball/Documents/audiobooks:/audiobooks
#      - </path/to/audiobooks>:/audiobooks
#      - </path/to/podcasts>:/podcasts
      - /docker/audiobookshelf_config:/config
      - /docker/audiobookshelf_metadata:/metadata
    environment:
      - TZ=America/New_York
