#LibreOffice
#https://hub.docker.com/r/linuxserver/libreoffice
#https://hub.docker.com/r/linuxserver/libreoffice
#https://docs.linuxserver.io/general/swag/#subdomain-proxy-conf

#Currently thinking of placing SWAG (or some other reverse proxy w/ auth) in front of LibreOffice.
# I have CloudFlare auth that I could use,but I would like an auth solution based directly in the app config
# ... and I will be reusing it anyway
